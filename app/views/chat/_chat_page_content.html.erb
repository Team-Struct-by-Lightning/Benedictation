        <!-- Page Content -->
        <div id="page-content-wrapper">
            <% flash.each do |message_type, message| %>
                <div class="alert alert-<%= message_type %>"><%= message %></div>
            <% end %>
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class='center-div'>
                        <h1>Hello <%= current_user.name %>, begin chatting!</h1>


                        <p>Use the sidebar on the left to create a new group or to enter a group that has already been created. Click below to start a chat without a group.
                        During a video chat, hold down <b>Cntrl-B</b> to address Benedict.

                        </div>
                        <% Relationship.where(user_id: current_user.id).pluck(:id).each do |relationship_id| %>
                        <% group = Relationship.find_by(id: relationship_id).group %>
                        <% group_id = group.id %>
                        <% group_name = group.group_name %>
                        <div class='center-div-group' id=<%="center-div" + group_id.to_s %> >
                            <h1>Hello <%= current_user.name %>, manage the group: <%= group_name %></h1>


                            <p>Use the sidebar on the left to create a new group or to enter a group that has already been created. Click below to start a chat without a group.
                            During a video chat, hold down <b>Cntrl-B</b> to address Benedict.

                        </div>
                        <% end %>


                        <div id="placeholder-div"></div>
                          <%= link_to "Start chatting",  "/room/" + get_random_string + "/" +
                          get_random_string, class: 'btn btn-primary' %>
                        <div id="participantsDiv"></div>


                        <!--<p>Make sure to keep all page content within the <code>#page-content-wrapper</code>.</p>-->
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-content-wrapper -->

        <script>

        $('.menu-group').click(function(e){
            var id = this.id;
            var id_array = id.split("menu-group");

            $('.center-div').hide();
            $('.center-div-group').hide();
            $('#center-div' + id_array[1]).show();
            // if (this.value % 2 == 1) {
            //     this.value += 1;
            //     console.log("why isn't it showing up!?")
            //     //var id = this.getAttribute("unique");
            //     //var str_id = id.toString();
            //     //$('#' + str_id).show();
            //     $('.add-user-button').fadeIn( "fast" );
            //     $('.go-to-room-button').fadeIn( "fast" );
            // }
            // else {
            //     this.value += 1;
            //     //var id = this.getAttribute("unique");
            //     //var str_id = id.toString();
            //     //$('#' + str_id).hide();
            //     $('.add-user-button').fadeOut( "fast" );
            //     $('.go-to-room-button').fadeOut( "fast" );
            // }
        });

        </script>