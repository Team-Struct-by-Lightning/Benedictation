  <script>

    $("#menu-toggle").click(function(e) {
        e.preventDefault();
        $("#wrapper").toggleClass("active");
    });


     $("").hover(
        function() {
        $(this).preappend( );
        }, function() {
            console.log("DONE");
    $( this ).find( "span:last" ).remove();
    });

    function minimize(string1, string2, string3){

      $(string1).slideToggle();
      console.log(string2);
      console.log(string1);
      console.log(string3);
      $(string2).hide();
      $(string3).show();
    }

    function take_spot(spot_num, video, spot_to_take){

      id = video.attr('id');
      for(i = 0; i < 6; i++){
        if(video_spots[i] == id){ //if video was already in list, remove it
            video_spots[i] = 0;
          }
        }

        video_spots[spot_num] = id;
        //alert(video_spots);
    }
    function get_free_spot(){
      if(video_spots[0] == 0){
        return "video-spot-1";
      }
      if(video_spots[1] == 0){
        return "video-spot-2";
      }
      if(video_spots[2] == 0){
        return "video-spot-3";
      }
      if(video_spots[3] == 0){
        return "video-spot-4";
      }
      if(video_spots[4] == 0){
        return "video-spot-5";
      }
    }

   </script>


<style>
#js-chat-body {
  max-height: 200px;
  overflow: auto;
  word-wrap: break-word;
}

#benny {
  margin-top: 0%;
  margin-left: 2%;
  margin-bottom: 2%;
}
#speech-rec {
  max-height: 250px;
  overflow: auto;
  word-wrap: break-word;
}

  video {
    height: 30%;
    width: 30%;
    margin-left: 1%;
    margin-right: 1%;
    margin-bottom: 1%;
    wrap: auto;
}

#js-input-im {
}
#js-send-button-im {
  height: 31px;
  margin-left: 5px;
  margin-top: -3px;
}

</style>